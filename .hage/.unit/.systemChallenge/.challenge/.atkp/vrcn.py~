#!/usr/bin/env python

#global through different files variable configuration, storage, and access through different files.
#other actions include commandError, forThree, Rid, CRASH, broadcast, and progAi.

def load():
	import pickle
	infile = open("memory","r")
	variables = pickle.load(infile)
	return variables
